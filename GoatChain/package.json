{
  "name": "goatchain",
  "version": "2.0.0",
  "description": "GoatChain – Standalone EVM-compatible blockchain contracts and tooling for trusted artist launches, staking and engagement.",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./artifacts ./cache",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network goatchain",
    "launch:artist": "hardhat run scripts/launchArtistToken.js --network goatchain",
    "stake": "hardhat run scripts/stake.js --network goatchain"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@openzeppelin/contracts-upgradeable": "^5.3.0",
    "@openzeppelin/contracts": "^5.3.0",
    "@openzeppelin/hardhat-upgrades": "^2.3.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.1"
  },
  "devDependencies": {
    "hardhat": "^2.19.4",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomiclabs/hardhat-etherscan": "^3.1.8",
    "hardhat-gas-reporter": "^1.0.10",
    "solidity-coverage": "^0.8.2",
    "rimraf": "^5.0.1"
  }
} 